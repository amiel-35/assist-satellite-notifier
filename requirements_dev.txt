homeassistant==2026.9.1
pytest-homeassistant-custom-component==0.13.364
pytest-cov
ruff
mypy
# This integration declares `dependencies: ["assist_satellite"]`, so Home
# Assistant imports and sets up the core `assist_satellite` component (and
# its own dependency chain: `assist_pipeline`, `conversation`, `stt`,
# `tts`) whenever a config entry loads -- including in tests. Those
# components import their Python requirements at module level, and
# `pytest-homeassistant-custom-component` does not install them. Versions
# pinned to what homeassistant==2026.9.1's manifests require.
hassil==3.12.0
gazetteer-matcher==1.1.0
home-assistant-intents==2026.8.28
pymicro-vad==1.0.1
pyspeex-noise==1.0.2
mutagen==1.48.1
ha-ffmpeg==3.2.2
